#!/bin/bash
set -exv

lists="$(find "/var/lib/apt/lists" \
			-maxdepth 1 \
			-type f \
			-name '*Release' \
			2>/dev/null)"
test -n "$lists" || apt-get update -q

apt-get install -qy "$@"

export cleanup
image-cleanup
